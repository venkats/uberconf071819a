export default class Airport {
  constructor(code, name, temperature, delay) {
    this.code = code;
    this.name = name;                
    this.temperature = temperature;
    this.delay = delay;
  }
}